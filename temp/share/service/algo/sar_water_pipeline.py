def run_full_pipeline(input_safe, graph_file, output_dir, skip_preprocessing=False):
    """Тестовая заглушка пайплайна обработки SAR"""
    print(f"[MOCK ALGO] Старт обработки файла: {input_safe}")
    print(f"[MOCK ALGO] Результаты будут сохранены в: {output_dir}")

    class Result:
        success = True
        error = None

    return Result()
